"use client";

// src/components/PageLayout.tsx
// Simplifié — la Navbar, Footer, AuthModal et AIChat sont maintenant dans GlobalShell (layout.tsx)
// Ce composant reste pour la compatibilité avec les pages existantes

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
